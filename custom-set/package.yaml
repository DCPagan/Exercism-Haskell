name: custom-set
version: 1.3.0.6

dependencies:
  - base
  - QuickCheck
  - bifunctors
  - data-fix
  - lens
  - mtl

library:
  exposed-modules:
    - CustomSet
    - RedBlackTree
    - Zipper
  source-dirs: src
  ghc-options: -Wall
  # dependencies:
  # - foo       # List here the packages you
  # - bar       # want to use in your solution.

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - custom-set
      - QuickCheck
      - hspec
